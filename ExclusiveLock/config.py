#assume that the max number of transaction is 10
lockedObj = []
for _ in range(10):
    lockedObj.append([])

#assume that the max number of query is 10
queue = []
for _ in range(10):
    queue.append([])